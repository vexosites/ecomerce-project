{
  "name": "ecomerce-project",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon --watch src --ext js,json --legacy-watch src/index.js",
    "start": "node src/index.js",
    "build": "tsc",
    "db:generate": "prisma generate",
    "db:migrate": "prisma migrate deploy",
    "db:push": "prisma db push",
    "setup": "npm run db:generate && npm run db:migrate",
    "prod": "npm run setup && npm run start"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "type": "module",
  "dependencies": {
    "@prisma/adapter-pg": "^7.3.0",
    "@prisma/client": "^7.2.0",
    "@prisma/extension-accelerate": "^3.0.1",
    "cloudinary": "^2.9.0",
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3",
    "multer": "^2.0.2",
    "redis": "^5.10.0",
    "redis-om": "^0.4.7"
  },
  "devDependencies": {
    "nodemon": "^3.1.11",
    "prisma": "^7.2.0",
    "wait-port": "^1.1.0"
  }
}
